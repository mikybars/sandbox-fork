spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/rent
    username: gabrielo
    password: gabrielo

  flyway:
    enabled: true
    locations: classpath:db/migration
    validate-on-migrate: true

  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.PostgreSQLDialect

logging:
  level:
    org:
      flywaydb: INFO

